class WishPoolFavorite {
  final int id;
  final int wishPoolId;
  final int userId;
  final DateTime createdAt;

  WishPoolFavorite({
    required this.id,
    required this.wishPoolId,
    required this.userId,
    required this.createdAt,
  });
}
