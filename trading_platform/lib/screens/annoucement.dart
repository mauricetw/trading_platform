import 'package:flutter/material.dart';

class Annoucement extends StatelessWidget {
  const Annoucement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('通知頁面'), // 之後要換成真正的通知列表
    );
  }
}